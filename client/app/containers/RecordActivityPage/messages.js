/*
 * RecordActivity Messages
 *
 * This contains all the text for the RecordActivity container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.RecordActivity";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the RecordActivity container!"
  }
});

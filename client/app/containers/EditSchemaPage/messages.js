/*
 * EditSchemaPage Messages
 *
 * This contains all the text for the EditSchemaPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.EditSchemaPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the EditSchemaPage container!"
  }
});

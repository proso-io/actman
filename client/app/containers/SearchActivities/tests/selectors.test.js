// import { selectSearchActivitiesDomain } from '../selectors';

describe("selectSearchActivitiesDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});

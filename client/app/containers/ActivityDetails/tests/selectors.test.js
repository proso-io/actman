// import { selectActivityDetailsDomain } from '../selectors';

describe("selectActivityDetailsDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});

/*
 * ActivityDetails Messages
 *
 * This contains all the text for the ActivityDetails container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ActivityDetails";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ActivityDetails container!"
  }
});
